# Arrow Functions

Different syntax for creating js functions

A normal function looks like:

```js
function myFnc() {}
```

An arrow function syntax:

```js
const myFnc = () => {};
```

It solves issues with the `this` keyword. It will always keep its context with arrow functions, unlike in traditional JS functions.
