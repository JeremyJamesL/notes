# Let and Const

Let & Const are different ways of creating variables.

Some variables never change, which are technically 'Constants'

`let` if you want to create a variable that really is a variable.

Use `const` for a constant value, i.e something that will never change. In React, you will never see `var`.
